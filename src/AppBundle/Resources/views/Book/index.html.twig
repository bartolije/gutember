{% extends 'AppBundle::base.html.twig' %}
{% block body %}
   <section class="row">

       <div class="col-xs-1 col-ms-3 col-md-4 col-md-offset-4 col-sm-offset-4">
    <table>
        <thead>

            <th>| Numéro |</th>
            <th>| nom |</th>
            <th>| description |</th>
            <th>| auteur |</th>
            <th>| nombre de page |</th>
            <th>| Action |</th>
        </thead>
        <tbody>
            {% for book in books %}
                <tr>
                <td>| {{ book.id }} |</td>
                <td>| {{ book.title }} |</td>
                <td>| {{ book.summary }} |</td>
                <td>| {{ book.author }} |</td>
                <td>| {{ book.pageCount }} |</td>
                <td>
                    <a href="{{ path('book_edit',{'id': book.id}) }}">Edit </a>
                    <a href="{{ path('book_delete',{'id': book.id}) }}">Delete <i class="fa fa-flask" aria-hidden="true"></i></a>
                </td>
                </tr>
             {% endfor %}
        </tbody>
    </table>
       </div>
       <div class="col-xs-1 col-ms-3 col-md-4"></div>
    </section>

{% endblock %}